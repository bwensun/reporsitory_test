关于localhost,本地IP,127.0.0.1
localhost
 是一个域名，在过去它指向 127.0.0.1 这个IP地址。在操作系统支持 ipv6 后，它同时还指向ipv6 的地址 [::1],在windows中被预定义，在host文件中被定义了映射关系

本机地址：
 本机物理网卡所绑定的网络协议地址，目前网络协议绝大多都是IPV4，故一般指的是IPV4地址。

 127.0.0.1：
 是一个特殊的网络接口，通常分配给loopback接口，用于本机中各个应用之间的交互，
 这只是一个默认的惯例。

